using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EachPlaysRandCard : MonoBehaviour//, CardInteractInterface
{
    /*
    private int players;
    private List<CardDeck> playerHands;
    private CardCreator cardMaker = GameObject.FindObjectOfType<CardCreator>();

    public EachPlaysRandCard(int numPlayers, List<CardDeck> hands)
    {
        players = numPlayers;
        playerHands = hands;
    }

    /*void Start()
    {
        players = GameInfo.GAMEINFO.NumPlayers;
        playerHands = CurrentGameInfo.CUR_GAME.PlayerHands;
    }*/
    /*
    public void onPlay()
    {
        for (int i = 0; i < players; i++)
        {
            //remove a card from each players deck
            string card = playerHands[i].draw();
            //playerHands[i].RemoveAt(playerHands.Count-1);
            cardMaker.createCard(card,i);
        }
    }
    */

}
